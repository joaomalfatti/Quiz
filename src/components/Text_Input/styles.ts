import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: 356,
    height: 57,
    marginBottom: -17,
    borderWidth: 4,
    borderRadius: 20,
    backgroundColor: "#FFFFFF",
    padding: 19,
    marginTop: 51,
    textAlign: "center",
  },
});
